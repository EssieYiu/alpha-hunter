# Alpha Hunter 开发协作规范

## 产品范围与已确认决策

- 用户已确认现有 `dist/` 交互原型，正式功能沿用其中文布局与工作流。保留原型可运行，正式代码放在 `apps/`。
- 本人使用，无登录、注册或应用鉴权。服务器入口绑定 loopback，通过 SSH 隧道访问。
- 指数覆盖 A 股、港股、美股及 `docs/dividend-indices.md` 中 7 个红利指数。允许最多 10 分钟行情延迟，不需要多年分钟历史。缺失数据明确提示，不使用合成数据伪装真实行情。
- 行情支持分时、日/周/月线，MA、EMA、BOLL、MACD、RSI、KDJ；分时价格线与指标线必须有明显形态区别。
- 自选、策略、模拟持仓流水、会话历史持久化到服务端。指数是研究标的，买卖是 ETF 模拟记账，不接真实券商订单。
- Agent 支持自定义兼容 API 服务和 Key、有界 ReAct 工具调用、全局侧栏、历史多轮对话、研究并创建策略、解读回测。**用户已明确要求 Agent 能新增策略**，不能沿用旧选型文档中“仅生成草稿”的限制；工具参数必须验证，创建过程与结果在对话中可查看。
- 回测为单标的、只做多、全仓/空仓的理论组合，支持 MA/EMA/RSI 日/周/月模板。信号在完成周期收盘形成，下一期开盘成交；记录成本、基准、净值、回撤和交易，保存不可变策略与行情快照。解释绑定该结果快照，不能编造未计算的指标。

## 技术与目录

- 依据 `docs/technical-selection.md`：React + TypeScript + Vite、React Router、TanStack Query；FastAPI + Pydantic、SQLAlchemy + Alembic、PostgreSQL、Redis + RQ。图表使用 Lightweight Charts。依赖需锁定。
- `apps/web/`：正式前端；`apps/api/`：后端路由、模型与领域服务；`contracts/`：接口契约；`infra/`：正式容器配置。`dist/` 与原有根部署脚本在正式版本验证完成前保持可用。
- API 统一 `/api` 前缀、JSON 请求；金额字段用十进制字符串。数据来源、行情时间和不可用原因必须进入响应。异常返回可展示的 detail，不包含凭据。
- API 和 worker 共用领域函数；Agent 通过同一服务访问数据，不能复制另一套账本或策略引擎。

## 协作方式

- 主 Agent 负责接口契约、集成验收与部署；子 Agent 只修改分配的目录。跨目录需求通过消息协调，不覆盖他人未提交改动。
- 第一轮分配：前端负责 `apps/web/`；后端核心负责 `apps/api/` 中行情、账户、策略、回测与数据库；Agent 服务由单独子任务负责 `apps/api/app/agent/`，接口先与后端协调。
- 先做可运行纵向流程，再扩展覆盖。任务结束报告修改文件、运行命令、验证结果、外部依赖和未完成项；不得将桩实现、模板回答或离线 fixtures 声称为正式能力。
- 不提交 Key、不写入日志、消息、队列或模型上下文。Key 仅本次请求使用；自定义模型 URL 使用配置的允许范围，不构建任意网络代理。
- 参数化策略不得执行任意用户/模型代码。用户新要求的代码策略单独走 `StrategyBase` 契约与受限子进程，仅供可信单人使用；Agent 不得提交代码策略。模拟账本使用 Decimal、数据库事务和幂等键，防止重复记账与超卖。

## 验收与部署

- 前端类型检查与生产构建通过；关键用户流程用隔离浏览器验证。
- 后端测试覆盖参数边界、指标口径、无未来数据回测、交易费用/现金/幂等、会话隔离和 Agent 工具验证。正式数据库集成使用 PostgreSQL；外部供应商探测单独执行，不混入确定性测试。
- 行情能力逐标的核验，10 分钟延迟按行情时间与交易日历判断，不按 HTTP 请求成功判断。
- Docker 部署目标 `root@8.216.46.125`，SSH 密钥已配置。保留 `build.sh` / `build.ps1` 一键发布接口，数据库用持久卷，发布不能删除用户数据。正式版本只有完成集成验证后才替换现有原型服务。
- 运行期间持续向用户简洁汇报，遇到真实外部依赖缺失明确说明；不重复索取已获授权。
